name: "[DEPRECATED] Drift Detection"

# This workflow has been deprecated and replaced by main.yml
# It exists only to prevent GitHub Actions failures for historical commits
# DO NOT USE - Will be removed after GitHub cache clears

on:
  push:
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation Notice
        run: |
          echo "::warning::This workflow has been DEPRECATED"
          echo "This workflow has been replaced by:"
          echo "  - .github/workflows/main.yml (main orchestrator with drift-check operation)"
          echo "  - .github/workflows/reusable-drift.yml (drift detection logic)"
          echo ""
          echo "To run drift detection, use:"
          echo "  gh workflow run main.yml -f operation=drift-check -f environment=all"
          echo ""
          echo "This stub will be removed once GitHub stops referencing it."
          exit 0